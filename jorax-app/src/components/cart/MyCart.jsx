import React from 'react';

function MyCart(props) {
    return (
        <section className='cart-section'>
           <h4>Nothing added in the cart !</h4> 
        </section>
    );
}

export default MyCart;